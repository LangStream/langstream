apiVersion: v1
kind: Service
metadata:
  name: {{ include "sga.fullname" . }}
  labels:
    {{- include "sga.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sga.selectorLabels" . | nindent 4 }}
