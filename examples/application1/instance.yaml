instance:
  globals:
    tableName: "vsearch.products"
  streamingCluster:
    type: "pulsar"
    configuration:
      credentials: "#pulsar-credentials"
      webServiceUrl: "https://pulsar.astra.datastax.com/"
      brokerUrl: "pulsar+tls://pulsar.astra.datastax.com:6651/"
      defaultTenant: "public"
      defaultNamespace: "default"
